import { NgNepaliDateDirective } from './ng-nepali-date.directive';

describe('NgNepaliDateDirective', () => {
  it('should create an instance', () => {
    const directive = new NgNepaliDateDirective();
    expect(directive).toBeTruthy();
  });
});
